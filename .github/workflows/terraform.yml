name: terraform

on:
  pull_request:
  push:
    branches: ["main"]
    paths:
      - "infra/**"
      - ".github/workflows/terraform.yml"

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  sandbox:
    name: Sandbox
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: infra/environments/sandbox

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.7.5"

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER_SANDBOX }}
          service_account: ${{ secrets.GCP_TF_SA_SANDBOX }}

      - uses: google-github-actions/setup-gcloud@v2

      - run: terraform init -input=false
      - run: terraform fmt -check -recursive
      - run: terraform validate
      - run: terraform plan -input=false -no-color -var-file=terraform.tfvars

      - name: Apply (main only)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: terraform apply -input=false -auto-approve -var-file=terraform.tfvars

  stag:
    name: Staging
    runs-on: ubuntu-latest
    needs: sandbox
    defaults:
      run:
        working-directory: infra/environments/stag

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.7.5"

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER_STAG }}
          service_account: ${{ secrets.GCP_TF_SA_STAG }}

      - uses: google-github-actions/setup-gcloud@v2

      - run: terraform init -input=false
      - run: terraform fmt -check -recursive
      - run: terraform validate
      - run: terraform plan -input=false -no-color -var-file=terraform.tfvars

      - name: Apply (main only)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: terraform apply -input=false -auto-approve -var-file=terraform.tfvars

  prod:
    name: Production
    runs-on: ubuntu-latest
    needs: stag
    defaults:
      run:
        working-directory: infra/environments/prod

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.7.5"

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER_PROD }}
          service_account: ${{ secrets.GCP_TF_SA_PROD }}

      - uses: google-github-actions/setup-gcloud@v2

      - run: terraform init -input=false
      - run: terraform fmt -check -recursive
      - run: terraform validate
      - run: terraform plan -input=false -no-color -var-file=terraform.tfvars

      - name: Apply (main only)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: terraform apply -input=false -auto-approve -var-file=terraform.tfvars